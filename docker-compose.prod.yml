# 猎鹰 Falcon AI · 生产 override
# 在基础 compose 之上追加 backend + frontend 容器服务。
#
# 用法：
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build

services:
  # 生产模式下重新收紧 postgres 的密码：若 .env 未设置 POSTGRES_PASSWORD 则拒绝启动。
  postgres:
    environment:
      POSTGRES_USER: ${POSTGRES_USER:?POSTGRES_USER required}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}
      POSTGRES_DB: ${POSTGRES_DB:?POSTGRES_DB required}
      TZ: Asia/Shanghai

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: falcon-backend:latest
    container_name: falcon-backend
    restart: unless-stopped
    environment:
      APP_ENV: production
      DEBUG: "false"
      # ---- 数据库 & 缓存：容器内走 compose 网络别名 ----
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:?POSTGRES_USER required}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}@postgres:5432/${POSTGRES_DB:?POSTGRES_DB required}
      REDIS_URL: redis://redis:6379/0
      STORAGE_ROOT: /app/storage
      MAX_UPLOAD_MB: ${MAX_UPLOAD_MB:-200}
      # LLM：可选，未配置则自动降级到规则式（生产默认使用火山方舟 doubao 模型）
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_BASE_URL: ${OPENAI_BASE_URL:-}
      LLM_MODEL: ${LLM_MODEL:-doubao-1-5-pro-32k-250115}
      TZ: Asia/Shanghai
    # 不再直接暴露端口，通过 Nginx 代理
    # ports:
    #   - "${BACKEND_PORT:-8000}:8000"
    volumes:
      - backend-storage:/app/storage
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - falcon-net

  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    image: falcon-nginx:latest
    container_name: falcon-nginx
    restart: unless-stopped
    ports:
      - "${NGINX_PORT:-80}:80"
    depends_on:
      - frontend
      - backend
    networks:
      - falcon-net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: falcon-frontend:latest
    container_name: falcon-frontend
    restart: unless-stopped
    environment:
      NODE_ENV: production
      TZ: Asia/Shanghai
    # 不再直接暴露端口，通过 Nginx 代理
    # ports:
    #   - "${FRONTEND_PORT:-3000}:3000"
    depends_on:
      - backend
    networks:
      - falcon-net

volumes:
  backend-storage:
